{
  "name": "marathon-docker-logs",
  "version": "0.2.0",
  "description": "Monitors Docker start/stop events, attaches to containers, streams their output to log files on disk.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sebastian Seilund <sebsei@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "docker-allcontainers": "^0.4.0"
  }
}
